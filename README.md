# timepass-project-

### Social preview
The page now declares the existing couple sticker as the Open Graph/Twitter share image, so link previews should use the couple sticker instead of the intro photo. Social platforms may cache an older preview; re-scraping/refreshing the URL may be required after deployment.
